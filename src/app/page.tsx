import { Button } from "@/components/ui/button";

const Home = () => {
  return <div>
    <Button variant="primary">Click me</Button>
    <p className="text-4xl font-bold text-red-500">Hello World</p>
  </div>;
};

export default Home;
